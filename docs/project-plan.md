# 任务省心记 APP 项目规划

> 状态：规划草案
>
> 版本：V0.1
>
> 更新时间：2026-07-27
>
> 当前阶段：项目定义
>
> 说明：当前没有产品需求素材。本文先基于项目名称建立可执行框架，所有关键假设均列入“待确认事项”。

## 1. 项目一句话定义

任务省心记是一款帮助用户快速记下任务、看清今天该做什么，并通过轻量提醒减少遗忘和心理负担的个人任务管理 APP。

它不追求成为复杂的项目管理工具。第一版只解决三个问题：

1. 想到一件事时，可以立即记下来。
2. 打开 APP 时，可以迅速判断下一步做什么。
3. 到了关键时间，能够收到不过度打扰的提醒。

## 2. 项目目标

### 2.1 用户目标

- 降低记录成本：用户不用先整理分类，也能在数秒内创建任务。
- 降低决策成本：首页直接呈现今天最需要处理的内容。
- 降低遗忘风险：重要任务可以设置时间，并获得可靠提醒。
- 提供完成感：完成操作明确、轻松，并能看到当日进展。

### 2.2 产品目标

- 完成一个可独立使用的 MVP（最小可用版本，即功能不多，但核心流程完整）。
- 验证“快速记录 + 今日聚焦 + 轻提醒”是否足以形成持续使用。
- 建立可扩展的设计规范和代码结构，后续能加入重复任务、清单协作等功能，而不推翻第一版。

### 2.3 暂不设定的目标

- 暂不以商业化、用户增长或社交传播作为第一阶段目标。
- 暂不追求覆盖团队项目管理、知识库、日历等全部效率场景。
- 暂不同时开发多个平台；先选择一个主平台完成闭环。

## 3. 目标用户与场景

### 3.1 初始目标用户

工作与生活任务较多，但不愿维护复杂任务系统的个人用户。

典型特征：

- 经常临时想到待办事项。
- 会因为任务分散在聊天、备忘录和脑海中而焦虑。
- 使用过待办工具，但容易因录入步骤多或分类复杂而放弃。
- 更需要“现在做什么”的答案，而不是精细的项目管理报表。

### 3.2 核心使用场景

| 场景 | 用户行为 | 产品应提供的结果 |
| --- | --- | --- |
| 临时想到一件事 | 快速输入一句话 | 任务立即保存，稍后可再整理 |
| 开始一天 | 打开首页 | 看见今天、逾期和最重要的任务 |
| 安排重要事项 | 设置日期或时间 | 任务进入对应列表并按时提醒 |
| 完成任务 | 点击完成 | 即时反馈，任务从当前列表移出 |
| 计划变化 | 延后或取消 | 少量操作即可完成，不要求重新创建 |

## 4. 产品原则

后续遇到功能取舍时，按以下顺序判断：

1. **先记录，再整理**：创建任务不能被分类、标签等设置阻断。
2. **首页给答案**：首页优先回答“我现在要做什么”，不堆积数据。
3. **默认简单，高级能力渐进出现**：常用操作直接可见，低频设置按需展开。
4. **提醒有价值，不制造噪音**：只有用户明确设置的关键时间才主动打扰。
5. **操作可恢复**：完成、删除、延后等高频操作应提供撤销机会。
6. **视觉服务于行动**：优先保证层级、可读性和触达效率，不用装饰掩盖信息。

## 5. MVP 范围

### 5.1 必须包含

#### A. 快速创建任务

- 从首页直接输入任务标题。
- 创建时允许不设置日期、时间和分类。
- 可选设置日期、具体时间、优先级和备注。
- 保存后立即出现在正确列表中。

#### B. 今日首页

- 展示今天的任务。
- 单独提示逾期任务。
- 支持完成、取消完成和延后。
- 空状态给出下一步操作，而不是只显示空白页面。

#### C. 任务列表

- 今天。
- 计划内：未来有日期的任务。
- 收集箱：尚未安排日期的任务。
- 已完成。

#### D. 任务详情

- 编辑标题、备注、日期、时间和优先级。
- 删除任务前说明影响，删除后短时间内可撤销。
- 记录完成状态。

#### E. 提醒

- 有明确时间的任务可设置提醒。
- 首次使用提醒时再申请系统通知权限。
- 用户拒绝权限后，APP 内仍能正常使用，并提供清晰的重新开启入口。

#### F. 基础设置

- 通知设置入口。
- 浅色、深色或跟随系统（具体首版支持范围待确认）。
- 基础数据说明与隐私说明。

### 5.2 第一版不做

- 团队协作、任务指派和成员权限。
- 聊天、评论和动态流。
- 复杂项目、甘特图、看板和工时统计。
- AI 自动拆解任务或大模型聊天助手。
- 跨应用自动化和开放 API。
- 游戏化积分、排行榜和社交分享。
- 多层级子任务、复杂标签体系和自定义字段。

这些能力不是永久排除，而是在核心使用价值被验证前不投入。

## 6. 信息架构

建议第一版使用四个一级入口：

```text
任务省心记
├── 今日
│   ├── 逾期任务
│   ├── 今日任务
│   └── 快速创建
├── 计划
│   ├── 未来任务
│   └── 日期筛选
├── 收集箱
│   └── 未安排任务
└── 我的
    ├── 已完成
    ├── 通知设置
    ├── 外观设置
    └── 关于与隐私
```

任务详情通过列表进入，不占用一级入口。

## 7. 核心用户流程

### 7.1 快速记录

打开 APP → 点击输入区或新增按钮 → 输入任务 → 保存 → 任务进入“收集箱”或指定日期列表。

验收重点：用户从打开首页到保存一个无日期任务，不超过 3 个主要动作。

### 7.2 安排并提醒

打开任务 → 选择日期 → 可选具体时间和提醒 → 保存 → 到达时间后收到系统通知 → 点击通知进入任务详情。

验收重点：设置日期不强制设置时间；设置提醒时必须让用户知道何时会被通知。

### 7.3 完成任务

在列表点击完成 → 显示轻量完成反馈 → 任务从当前未完成列表移出 → 短时间展示“撤销”。

验收重点：反馈立即出现；误触后可以恢复。

### 7.4 延后任务

对任务执行延后 → 选择常用时间或自定义日期 → 任务移动到新日期 → 当前列表同步更新。

验收重点：常用选项建议包括“明天”和“下周”，具体选项在原型测试中确认。

## 8. 页面规划

| 页面 | 核心任务 | 关键内容 | 优先级 |
| --- | --- | --- | --- |
| 今日首页 | 判断今天做什么 | 逾期、今日任务、快速创建、完成反馈 | P0 |
| 新建任务 | 快速记录 | 标题、日期、时间、优先级、备注 | P0 |
| 任务详情 | 查看和修改任务 | 完整字段、完成、延后、删除 | P0 |
| 计划 | 查看未来安排 | 按日期分组的未来任务 | P0 |
| 收集箱 | 整理未安排事项 | 无日期任务、快速安排日期 | P0 |
| 已完成 | 回看已完成事项 | 完成时间、恢复任务 | P1 |
| 设置 | 管理通知和外观 | 权限状态、主题、隐私说明 | P1 |
| 首次引导 | 解释核心价值 | 最少步骤的价值说明与权限引导 | P1 |

优先级说明：P0 是首版闭环不可缺少；P1 是重要但可在核心流程稳定后补齐。

## 9. 交互与视觉规划

### 9.1 交互要求

- 高频操作单手可达，主要新增入口保持稳定。
- 列表项默认只展示判断任务所需的信息，备注等内容进入详情查看。
- 滑动手势只能作为快捷方式，同一操作必须有可发现的按钮入口。
- 删除等不可逆操作需要二次确认或可靠撤销。
- 加载、空状态、错误、无网络和权限被拒绝都要有明确反馈。
- 动效只解释状态变化，不能拖慢连续完成任务的操作。

### 9.2 首轮视觉方向

- 气质：轻松、清楚、可靠，避免企业项目管理工具的压迫感。
- 信息密度：中低密度，首页留出呼吸感，但不牺牲任务浏览效率。
- 色彩：中性色作为主体，只用一个品牌强调色表示主要行动。
- 状态不能只依赖颜色，还需结合图标、文字或形状，保证可访问性。
- 正文和关键操作满足移动端可读性与触控尺寸要求。

### 9.3 需要建立的设计规范

- 颜色、字体、间距、圆角、阴影等基础变量。
- 按钮、输入框、任务卡片、日期选择、弹窗、提示条等组件。
- 组件的默认、按下、禁用、错误、完成等状态。
- 空状态、加载状态、错误状态和权限状态模板。

## 10. 数据与技术规划

本节只定义能力边界，具体技术选型在原型确认后单独形成技术方案。

### 10.1 核心数据对象

一个任务至少需要：

| 字段 | 用途 |
| --- | --- |
| `id` | 唯一识别任务 |
| `title` | 任务标题 |
| `note` | 可选备注 |
| `dueDate` | 可选日期 |
| `dueTime` | 可选具体时间 |
| `priority` | 可选优先级 |
| `status` | 未完成或已完成 |
| `reminderAt` | 可选提醒时间 |
| `createdAt` | 创建时间 |
| `updatedAt` | 最后修改时间 |
| `completedAt` | 可选完成时间 |

### 10.2 技术能力要求

- 本地数据可靠保存，退出和重启后不丢失。
- 日期、时区和跨天状态计算准确。
- 系统通知支持创建、修改和取消。
- 为未来登录和多设备同步预留数据结构，但第一版是否做云同步待确认。
- 关键错误可被记录，且不包含用户任务正文等敏感信息。

### 10.3 质量底线

- 不在代码、文档或日志中保存密钥。
- 用户任务数据默认视为隐私信息。
- 数据删除、备份和导出策略需在公开测试前明确。
- 关键流程需要自动化测试，交互和视觉需要真机验证。

## 11. 阶段与里程碑

当前不填具体日期，先按交付物和通过条件推进，避免在需求未确认时制造虚假排期。

### 阶段 0：项目定义

交付物：

- 项目规划 V1.0。
- 目标用户和核心问题定义。
- MVP 范围确认。
- 主平台确认。

通过条件：待确认事项中的产品方向问题已得到回答，P0 范围不再存在根本分歧。

### 阶段 1：产品结构与低保真原型

交付物：

- 用户流程。
- 信息架构。
- P0 页面低保真原型。
- 核心状态清单。

通过条件：无需视觉细节，也能完整演示创建、安排、提醒、完成和延后流程。

### 阶段 2：视觉设计与可用性验证

交付物：

- 视觉方向。
- 基础设计规范。
- P0 页面高保真原型。
- 一轮可用性测试记录及修改结果。

通过条件：目标用户无需讲解即可完成核心任务，阻断性问题已修复。

### 阶段 3：MVP 开发

交付物：

- 可安装或可访问的测试版本。
- 核心功能。
- 基础测试。
- 已知问题清单。

通过条件：P0 验收用例全部通过，没有导致数据丢失或核心流程中断的问题。

### 阶段 4：内测与优化

交付物：

- 内测反馈汇总。
- 问题优先级列表。
- 性能、稳定性和易用性优化。
- 发布检查清单。

通过条件：高优先级问题关闭，通知、日期和数据保存经过连续使用验证。

### 阶段 5：发布准备

交付物：

- 产品介绍、隐私说明、使用说明。
- 版本号和更新记录。
- 数据备份或迁移预案。
- 发布版本。

通过条件：需要公开发布时，必须由项目负责人单独确认后执行。

## 12. MVP 验收标准

### 12.1 功能验收

- 用户可以创建无日期任务，任务进入收集箱。
- 用户可以创建有日期任务，任务出现在正确日期。
- 用户可以修改任务，所有入口显示一致结果。
- 用户可以完成、恢复、延后和删除任务。
- 有时间的任务可以正常触发、修改和取消提醒。
- APP 重启后数据保持一致。
- 跨天后，昨日未完成任务能被正确识别为逾期。

### 12.2 体验验收

- 新用户无需教程即可创建并完成一个任务。
- 核心流程没有死路，每种状态都能继续下一步。
- 误操作有明确反馈，重要操作可以撤销或确认。
- 常见手机尺寸下，不出现文字遮挡、按钮难以点击或内容无法滚动。
- 深色模式若进入首版，则所有 P0 页面和系统状态都必须完整适配。

### 12.3 稳定性验收

- 不出现任务无故丢失、重复或状态回退。
- 通知权限变化后，APP 内显示与系统真实状态一致。
- 异常退出后重新打开，不破坏已保存数据。
- 已知严重问题为零，普通问题有明确记录和处理优先级。

## 13. 关键风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 功能不断增加 | 首版迟迟无法完成 | 新需求先进入候选池，不直接加入 P0 |
| 首页信息过多 | 用户仍不知道先做什么 | 以“下一步行动”作为首页唯一核心指标 |
| 日期与提醒逻辑复杂 | 发生漏提醒或错误提醒 | 先定义规则和边界用例，再实现界面 |
| 设计先于需求收敛 | 反复返工 | 先完成低保真流程，再投入高保真 |
| 多平台同时开发 | 测试与维护成本过高 | 第一版只选一个主平台 |
| 过早加入 AI | 成本增加但核心价值不清 | 核心流程稳定并获得真实需求后再评估 |

## 14. 待确认事项

以下问题会直接影响产品范围，需要在进入低保真原型前确认：

1. 首发平台是 iOS、Android、Web，还是跨平台 APP？
2. 产品只给个人使用，还是从一开始就考虑公开发布？
3. 是否需要登录和多设备同步？
4. 是否必须离线可用？
5. “省心记”最关键的差异点是快速记录、智能提醒，还是自动整理？
6. 首版是否需要重复任务？
7. 首版是否需要子任务或简单清单？
8. 是否存在你已经确定的视觉风格、品牌色或参考产品？
9. 是否有明确的目标完成时间或每周可投入时间？
10. 成功的判断依据是什么：自己持续使用、完成一次公开发布，还是获得一定数量的测试用户？

## 15. 下一步

按以下顺序推进：

1. 回答“待确认事项”，将本文收敛为 V1.0。
2. 编写 MVP PRD，逐项定义功能规则和异常情况。
3. 绘制核心用户流程与页面结构。
4. 制作低保真原型，先验证操作流程。
5. 原型通过后，再建立视觉规范并制作高保真界面。
6. 高保真确认后，编写技术方案和开发任务清单。

## 16. 版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| V0.1 | 2026-07-27 | 基于项目名称建立首版规划框架，等待产品方向确认 |
